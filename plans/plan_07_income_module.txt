Expense Tracker - Module 4: Income Management Module Plan
===========================================================

Module Name: Income Management
File: plan_07_income_module.txt

---


Overview
--------
Allows users to create, view, edit, delete, and search their income transactions.
This module mirrors the Expense Management module but handles income instead of expenses.
Users can filter income by date, category, and amount.

NOTE: This module is structurally identical to the Expense module except:
   - Transaction type is 'income' instead of 'expense'
   - Only income categories are used
   - Amounts are displayed in green (positive) instead of red
   - No receipt upload for income (optional, can be added if needed)

---


Pages / Views
-------------

1. Income List Page
   Path: views/incomes/index.php (or list.php)
   Shows: filter form, income table, pagination

2. Create Income Page
   Path: views/incomes/create.php
   Shows: income creation form with all fields

3. Edit Income Page
   Path: views/incomes/edit.php
   Shows: pre-filled form to edit an existing income

---


Controllers
-----------

1. IncomeController.php
   Methods:
   - index()        -> show list page with filters and pagination
   - create()       -> show create form
   - store()        -> handle POST, validate, save income
   - edit($id)      -> show edit form with pre-filled data
   - update($id)    -> handle PUT/PATCH, validate, update income
   - destroy($id)   -> delete income
   - getCategories() -> AJAX: get income categories for dropdown

---


Models Used
-----------

1. Transaction.php
   Methods:
   - createIncome($user_id, $data)
   - updateIncome($id, $user_id, $data)
   - deleteIncome($id, $user_id)
   - findById($id, $user_id)
   - getAllIncomes($user_id, $filters, $page, $perPage)
   - getIncomeCount($user_id, $filters)

2. Category.php
   Methods:
   - getIncomeCategories($user_id)
   - getById($user_id, $id)

---


Income List Page Details
--------------------------

Filter Form (top of page):
   - Date From: date input
   - Date To: date input
   - Category: dropdown (all income categories)
   - Amount From: number input
   - Amount To: number input
   - Search: text input (search in description)
   - Reset Filters button
   - Apply Filters button

Table Columns:
   - Checkbox (for bulk delete, optional)
   - Date
   - Description
   - Category (with color indicator)
   - Amount (green color, formatted with + sign)
   - Actions: Edit button, Delete button

Features:
   - Sortable columns (click header to sort by date/amount/category)
   - Pagination (10, 25, 50, 100 per page options)
   - "Add New Income" button at top
   - Empty state message if no income records
   - Bulk delete with confirmation (optional)

---


Create Income Page Details
---------------------------

Form Fields:
   - Amount: number input, required, min 0.01, step 0.01
   - Category: dropdown, required, populated with user's income categories
   - Date: date input, required, default today's date
   - Description: textarea, optional, max 500 chars
   - Source: text input, optional, max 100 chars (where income came from)
   - Submit button
   - Cancel button (redirects to income list)

Validation:
   - Amount: required, numeric, > 0
   - Category: required, must be an income category owned by user
   - Date: required, valid date, cannot be future date (optional)
   - Description: max 500 chars, optional
   - Source: max 100 chars, optional

Success: Redirect to income list with success flash message
Failure: Redirect back with errors, retain form data

---


Edit Income Page Details
-------------------------

Same form as create, pre-filled with existing data:
   - Load income by id and user_id
   - Show 404 if not found or not owned by user
   - Pre-fill all fields
   - Submit button says "Update Income"
   - Delete button at bottom (with confirmation modal)

---


AJAX Endpoints
--------------

1. GET /incomes/categories
   Purpose: Get income categories for dropdown
   Response: JSON array of {id, name, color}

---


Middleware
----------

- AuthMiddleware: required
- CSRF Middleware: required for all POST requests

---


Validation Rules
----------------

Store/Update:
- amount: required|numeric|min:0.01
- category_id: required|exists:categories,id|type:income
- date: required|date|before_or_equal:today (optional)
- description: nullable|string|max:500
- source: nullable|string|max:100

---


Flash Messages
--------------

Success:
- "Income added successfully"
- "Income updated successfully"
- "Income deleted successfully"

Error:
- "Failed to add income. Please try again."
- "Failed to update income. Please try again."
- "Failed to delete income. Please try again."
- "You do not have permission to perform this action"

---


Routes
------

GET    /incomes                    -> IncomeController@index
GET    /incomes/create             -> IncomeController@create
POST   /incomes                    -> IncomeController@store
GET    /incomes/{id}/edit          -> IncomeController@edit
PUT    /incomes/{id}               -> IncomeController@update
DELETE /incomes/{id}               -> IncomeController@destroy
GET    /incomes/categories         -> IncomeController@getCategories (AJAX)

---


Assets Used
-----------

CSS: assets/css/incomes.css
JS:  assets/js/incomes.js

---


Query Parameters for Filtering
-------------------------------

- date_from: YYYY-MM-DD
- date_to: YYYY-MM-DD
- category_id: integer
- amount_from: decimal
- amount_to: decimal
- search: string
- sort: date|amount|category (default: date)
- direction: asc|desc (default: desc)
- page: integer (default: 1)
- per_page: 10|25|50|100 (default: 10)

---


Sorting Behavior
----------------

Default: Sort by date descending (newest first)
Options:
- Click "Date" header: toggle asc/desc
- Click "Amount" header: toggle asc/desc
- Click "Category" header: sort alphabetically

---


Pagination Details
------------------

- Show: "Showing 1-10 of 25 results"
- Page numbers with prev/next
- Current page highlighted
- Disabled prev on first page, disabled next on last page
- Preserve filter parameters in pagination links

---


Empty States
------------

- No income at all: "You have no income records yet. Start by adding your first income!"
- No income match filters: "No income records match your filters. Try adjusting your search criteria."
- No categories: "No income categories found. Please create categories first."

---


Differences from Expense Module
--------------------------------

1. Transaction type is 'income'
2. Only income categories allowed
3. Amounts displayed in green with + prefix
4. No receipt upload functionality
5. Dashboard shows income in green cards
6. Reports show income in green charts
7. URL prefix is /incomes instead of /expenses
8. Controller is IncomeController instead of ExpenseController

---


Access Control
--------------

- All authenticated users can manage their own income
- Users cannot view/edit/delete other users' income
- Hard delete with cascade (if any related records exist)

---


IMPORTANT: Security Implementation Standards
==============================================

This module MUST follow these security practices in implementation:

1. Prepared Statements: ALL database queries MUST use PDO prepared statements with bound parameters. Never concatenate user input into SQL queries.
   Example: $stmt = $pdo->prepare("SELECT * FROM transactions WHERE user_id = :user_id AND date >= :date_from");
            $stmt->execute(['user_id' => $user_id, 'date_from' => $date_from]);

2. Input Sanitization: ALL user inputs must be sanitized before validation and database insertion.
   Example: $amount = filter_input(INPUT_POST, 'amount', FILTER_SANITIZE_NUMBER_FLOAT, FILTER_FLAG_ALLOW_FRACTION);
            $description = filter_input(INPUT_POST, 'description', FILTER_SANITIZE_SPECIAL_CHARS);

3. Output Escaping: ALL dynamic content rendered in views MUST be escaped using htmlspecialchars() to prevent XSS.
   Example: echo htmlspecialchars($transaction['description'], ENT_QUOTES, 'UTF-8');
            echo htmlspecialchars($category['name'], ENT_QUOTES, 'UTF-8');

4. Authorization Checks: Verify the logged-in user owns the resource before any operation.
   Example: if ((int)$transaction['user_id'] !== (int)$_SESSION['user_id']) { http_response_code(403); exit('Forbidden'); }

5. CSRF Protection: All POST/PUT/DELETE forms must include and validate CSRF tokens.
   Example: if (!hash_equals($_SESSION['csrf_token'], $_POST['csrf_token'])) { http_response_code(403); exit('Invalid CSRF token'); }

6. File Upload Security: Validate MIME type by extension and content, use random filenames, store outside web root if possible.
   Example: $allowed = ['jpg','jpeg','png','gif'];
            $ext = strtolower(pathinfo($_FILES['file']['name'], PATHINFO_EXTENSION));
            if (!in_array($ext, $allowed)) { reject; }

---


Testing Checklist
-----------------

- [ ] Income list loads with all income records
- [ ] Filters work correctly (date, category, amount, search)
- [ ] Sorting works (date, amount, category)
- [ ] Pagination works and preserves filters
- [ ] Create income form validates correctly
- [ ] Income is created and redirects to list
- [ ] Edit income pre-fills all fields correctly
- [ ] Update income saves changes
- [ ] Delete income removes record
- [ ] Cannot view/edit/delete other user's income
- [ ] Bulk delete works (if implemented)
- [ ] CSRF protection works
- [ ] XSS prevention in description field
- [ ] Responsive design on mobile
- [ ] Amounts display in green with currency format
